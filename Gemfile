# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

gem "activesupport"
gem "gemoji", "< 4" # v4 removes emoji images, which we need
gem "mini_magick"
gem "unicode-emoji", require: "unicode/emoji"
